# Client SOAP — doar requests, ca să rămânem portabili pe Python nou.
requests>=2.31

# API + portal web (Faza 5+6)
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1

# Stocarea (Faza 3) folosește sqlite3 din standard library — fără dependență.

# Dezvoltare
pytest>=8.0
